import 'package:flutter/cupertino.dart';

class SearhPage extends StatelessWidget {
  const SearhPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child:Text(
            'Search Page'
        ),
      ),
    );
  }
}